## QA Review — 2026-05-08 v1.95.4
### Метрики: Python=920 тестов (2 skipped) | Coverage=80% | Vitest=223 теста | Total test defs=915
| # | Замечание | 🔴/🟡/🟢 |
|---|---|---|
| 1 | **Неполное тестовое покрытие (80%)**: 20% кода бэкенда не покрыто тестами, что создает риски пропустить баги в новой функциональности или регрессии. | 🔴 |
| 2 | **E2E тесты не выполняются в рамках аудита**: В `ui/package.json` есть скрипт `test:e2e`, но он не входит в стандартный набор команд для QA-аудита. Отсутствие регулярного прогона сквозных тестов — серьезный пробел. | 🔴 |
| 3 | **Отсутствие нагрузочного тестирования**: Директория `tests/load` существует, но тесты в ней отсутствуют или не запускаются. Для сервиса обработки видео критически важно понимать поведение системы под нагрузкой. | 🔴 |
| 4 | **Ошибки в логах API во время тестов**: Зафиксирована ошибка `Неожиданная ошибка при создании проекта` в `projects.py`. Несмотря на то, что тесты прошли, наличие `ERROR` в логах тестового прогона недопустимо. | 🔴 |
| 5 | **"Шумные" логи при тестировании**: Вывод `coverage report` переполнен логами уровня `WARNING` и `ERROR` (например, `retry.exhausted`, `api.pipeline_error`), что затрудняет анализ результатов. | 🟡 |
| 6 | **Пропущенные тесты (2)**: Два unit-теста пропущены (`skipped=2`). Необходимо выяснить причину: они устарели, нестабильны или для них еще не реализована функциональность? | 🟡 |
| 7 | **Потенциально нестабильные retry-механизмы**: Логи тестов изобилуют сообщениями о повторных попытках (`retry.attempt`) и их исчерпании (`retry.exhausted`), особенно в `TestSpeechkitTTS`. Это может указывать на нестабильность интеграций. | 🟡 |
| 8 | **Зависимость от `ruaccent`**: В логах тестов есть предупреждение `stress.ruaccent_unavailable`. Если пакет опционален, тесты не должны выводить warning в его отсутствие. Если обязателен — он должен быть в зависимостях. | 🟡 |
| 9 | **Устаревшие отчеты о ручном тестировании**: В `docs/reports` лежат отчеты по UI за начало мая. Нет ясности, были ли исправлены найденные проблемы и каков процесс работы с ними. | 🟡 |
| 10 | **Небольшое расхождение в количестве тестов**: `unittest` рапортует о 920 тестах, тогда как поиск по `def test_` находит 915. Разница, вероятно, в параметризованных тестах, но это стоит задокументировать для прозрачности. | 🟢 |

**Вердикт: БЛОК**
